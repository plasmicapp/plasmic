/** This file is auto-generated by Plasmic; please do not edit! */
import { useRouter } from "next/router";
import PlasmicLoader, { getPageUrls, getComponentFromUrl } from "@plasmicapp/loader";

export default function PlasmicLoaderPage() {
  const router = useRouter();
  const { name, projectId } = getComponentFromUrl(router.query.plasmicLoaderPage);
  return <PlasmicLoader component={name} projectId={projectId} />;
}

export async function getStaticProps() {
  return {
    props: {},
  };
}

export async function getStaticPaths() {
  return {
    paths: getPageUrls().map((url) => ({
      params: { plasmicLoaderPage: url.substring(1).split("/") },
    })),
    fallback: false,
  };
}
