// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicButton,
} from "./plasmic/plain_kit/PlasmicButton";
import { PlankButtonProps, PlankButtonRef, usePlankButton } from "@plasmicapp/plank";

interface ButtonProps extends PlankButtonProps {
}

const Button = React.forwardRef(function Button(props: ButtonProps, ref: PlankButtonRef) {
  const {plankProps} = usePlankButton(
    PlasmicButton,
    props,
    {
      isDisabledVariant: ["isDisabled", "isDisabled"],
      showStartIconVariant: ["withIcons", "start"],
      showEndIconVariant: ["withIcons", "end"],
      root: "root"
    },
    ref
  );
  return <PlasmicButton {...plankProps} />;
});

export default Button;
