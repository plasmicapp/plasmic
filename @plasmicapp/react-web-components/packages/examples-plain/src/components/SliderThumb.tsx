// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicSliderThumb,
} from "./plasmic/plain_kit/PlasmicSliderThumb";
import { PlankSliderThumbProps, usePlankSliderThumb, PlankSliderThumbRef } from "@plasmicapp/plank";

interface SliderThumbProps extends PlankSliderThumbProps {}

const SliderThumb = React.forwardRef(function SliderThumb(props: SliderThumbProps, ref: PlankSliderThumbRef) {
  const {plankProps} = usePlankSliderThumb(
    PlasmicSliderThumb,
    props,
    {
      isDisabledVariant: ["isDisabled", "isDisabled"],
      isDraggingVariant: ["isDragging", "isDragging"],
      hasLabelVariant: ["hasLabel", "hasLabel"],
      root: "root",
      label: "labelContainer"
    },
    ref
  );
  return <PlasmicSliderThumb {...plankProps} />;
});

export default SliderThumb;
