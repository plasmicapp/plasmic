// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import {
  DefaultPeriodPickerProps,
  PlasmicPeriodPicker,
} from "@/wab/client/plasmic/plasmic_kit_analytics/PlasmicPeriodPicker";
import { HTMLElementRefOf } from "@plasmicapp/react-web";
import * as React from "react";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface PeriodPickerProps extends Omit<DefaultPeriodPickerProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultPeriodPickerProps altogether and have
// total control over the props for your component.
export interface PeriodPickerProps extends DefaultPeriodPickerProps {
  period: string;
  setPeriod: (_: string) => void;
}

function PeriodPicker_(props: PeriodPickerProps, ref: HTMLElementRefOf<"div">) {
  const { period, setPeriod, ...rest } = props;

  return (
    <PlasmicPeriodPicker
      root={{ ref }}
      month={period === "month"}
      {...rest}
      dayOption={{
        onClick: () => setPeriod("day"),
      }}
      monthOption={{
        onClick: () => setPeriod("month"),
      }}
    />
  );
}

const PeriodPicker = React.forwardRef(PeriodPicker_);
export default PeriodPicker;
