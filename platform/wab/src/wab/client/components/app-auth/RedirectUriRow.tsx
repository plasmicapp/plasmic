// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import {
  DefaultRedirectUriRowProps,
  PlasmicRedirectUriRow,
} from "@/wab/client/plasmic/plasmic_kit_end_user_management/PlasmicRedirectUriRow";
import { HTMLElementRefOf } from "@plasmicapp/react-web";
import * as React from "react";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface RedirectUriRowProps extends Omit<DefaultRedirectUriRowProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultRedirectUriRowProps altogether and have
// total control over the props for your component.
export interface RedirectUriRowProps extends DefaultRedirectUriRowProps {
  uri: string;
  onChange: (uri: string) => void;
  onRemove: () => void;
}

function RedirectUriRow_(
  props: RedirectUriRowProps,
  ref: HTMLElementRefOf<"div">
) {
  const { uri, onChange, onRemove, ...rest } = props;

  return (
    <PlasmicRedirectUriRow
      root={{ ref }}
      textBox={{
        value: uri,
        placeholder: "https://example.com",
        onChange: (e) => {
          onChange(e.target.value);
        },
      }}
      removeBtn={{
        onClick: onRemove,
      }}
      {...rest}
    />
  );
}

const RedirectUriRow = React.forwardRef(RedirectUriRow_);
export default RedirectUriRow;
