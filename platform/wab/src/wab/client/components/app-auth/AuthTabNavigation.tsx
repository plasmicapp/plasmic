// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import { HTMLElementRefOf } from "@plasmicapp/react-web";
import * as React from "react";
import {
  DefaultAuthTabNavigationProps,
  PlasmicAuthTabNavigation,
} from "../../plasmic/plasmic_kit_end_user_management/PlasmicAuthTabNavigation";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface AuthTabNavigationProps extends Omit<DefaultAuthTabNavigationProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultAuthTabNavigationProps altogether and have
// total control over the props for your component.
export interface AuthTabNavigationProps extends DefaultAuthTabNavigationProps {
  changeTab: (tab: string) => void;
}

function AuthTabNavigation_(
  props: AuthTabNavigationProps,
  ref: HTMLElementRefOf<"div">
) {
  const { changeTab, ...rest } = props;

  return (
    <PlasmicAuthTabNavigation
      root={{ ref }}
      permissions={{
        onClick: () => changeTab("permissions"),
      }}
      settings={{
        onClick: () => changeTab("settings"),
      }}
      activity={{
        onClick: () => changeTab("activity"),
      }}
      {...rest}
    />
  );
}

const AuthTabNavigation = React.forwardRef(AuthTabNavigation_);
export default AuthTabNavigation;
