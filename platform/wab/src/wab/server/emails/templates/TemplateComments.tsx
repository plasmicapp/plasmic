// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import {
  DefaultTemplateCommentsProps,
  PlasmicTemplateComments,
} from "@/wab/server/emails/templates/plasmic/plasmic_kit_email_templates/PlasmicTemplateComments";
import * as React from "react";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface TemplateCommentsProps extends Omit<DefaultTemplateCommentsProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultTemplateCommentsProps altogether and have
// total control over the props for your component.

type ReplyData = {
  rootComment: {
    id: string;
    body: string;
    name: string;
    avatarUrl?: string | null;
  };
  link: string;
  replies: {
    name: string;
    comment: string;
    avatarUrl?: string | null;
  }[];
};

type ReactionData = {
  commentId: string;
  comment: string;
  link: string;
  reactions: {
    name: string;
    emoji: string;
  }[];
};

type ResolutionData = {
  name: string;
  resolved: boolean;
  link: string;
  rootComment: {
    name: string;
    body: string;
    avatarUrl?: string | null;
  };
};

type CommentData = {
  name: string;
  link: string;
  commentId: string;
  comment: string;
  avatarUrl?: string | null;
};

export interface TemplateCommentsProps extends DefaultTemplateCommentsProps {
  mentions: CommentData[];
  comments: CommentData[];
  replies: ReplyData[];
  reactions: ReactionData[];
  resolutions: ResolutionData[];
}

function TemplateComments(props: TemplateCommentsProps) {
  // Use PlasmicTemplateComments to render this component as it was
  // designed in Plasmic, by activating the appropriate variants,
  // attaching the appropriate event handlers, etc.  You
  // can also install whatever React hooks you need here to manage state or
  // fetch data.
  //
  // Props you can pass into PlasmicTemplateComments are:
  // 1. Variants you want to activate,
  // 2. Contents for slots you want to fill,
  // 3. Overrides for any named node in the component to attach behavior and data,
  // 4. Props to set on the root node.
  //
  // By default, we are just piping all TemplateCommentsProps here, but feel free
  // to do whatever works for you.

  return <PlasmicTemplateComments {...props} />;
}

export default TemplateComments;
