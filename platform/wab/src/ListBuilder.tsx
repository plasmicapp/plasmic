// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import { ReactNode } from "react";
// eslint-disable-next-line @typescript-eslint/no-restricted-imports
import {
  DefaultListBuilderProps,
  PlasmicListBuilder,
  PlasmicListBuilder__OverridesType,
} from "@/wab/client/plasmic/plasmic_kit_data_queries/PlasmicListBuilder";

interface ListBuilderProps
  extends DefaultListBuilderProps,
    Pick<PlasmicListBuilder__OverridesType, "addButton"> {
  onIconButtonPress?: () => void;
  icon?: ReactNode;
}

function ListBuilder({ onIconButtonPress, ...props }: ListBuilderProps) {
  return (
    <PlasmicListBuilder
      {...props}
      addButton={
        props.addButton ?? {
          onClick: onIconButtonPress,
        }
      }
    />
  );
}

export default ListBuilder;
